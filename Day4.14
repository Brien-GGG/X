#include <iostream>
#include <string>

int main()
{
    std::string(str);
    std::cout << "Enter a string:";
    get(std::cin, str);

    int max=0;
    char symb;
    int count=0;
    int size=str.length;

    for(int i=0; I<
}
